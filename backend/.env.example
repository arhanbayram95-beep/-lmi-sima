PORT=3000
GEMINI_API_KEY=
# Optional — see server.ts's dual-key failover split. Every reading
# request retries across both keys if set; falls back to GEMINI_API_KEY
# alone for every module if left unset.
GEMINI_API_KEY_SECONDARY=
REVENUECAT_API_KEY=
